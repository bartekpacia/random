This is a sample project to test resolving definitions of steps defined in a external library.

Build a JAR:

```shell
mvn package clean
```

Move it to the right place:

```shell
cp target/steps-1.0.0.jar 
```