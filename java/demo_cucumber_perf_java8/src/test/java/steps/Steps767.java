package steps;

import io.cucumber.java8.En;

public class Steps767 implements En {

    public Steps767() {
        When("^the step is implemented767$", () -> {});
        When("^the step is not implemented yet767$", () -> {});
        Then("^the next step is executed767$", () -> {});
        Then("^the next step is skipped767$", () -> {});
    }
}
