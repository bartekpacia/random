package steps;

import io.cucumber.java8.En;

public class Steps550 implements En {

    public Steps550() {
        When("^the step is implemented550$", () -> {});
        When("^the step is not implemented yet550$", () -> {});
        Then("^the next step is executed550$", () -> {});
        Then("^the next step is skipped550$", () -> {});
    }
}
