package steps;

import io.cucumber.java8.En;

public class Steps196 implements En {

    public Steps196() {
        When("^the step is implemented196$", () -> {});
        When("^the step is not implemented yet196$", () -> {});
        Then("^the next step is executed196$", () -> {});
        Then("^the next step is skipped196$", () -> {});
    }
}
