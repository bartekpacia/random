package steps;

import io.cucumber.java8.En;

public class Steps644 implements En {

    public Steps644() {
        When("^the step is implemented644$", () -> {});
        When("^the step is not implemented yet644$", () -> {});
        Then("^the next step is executed644$", () -> {});
        Then("^the next step is skipped644$", () -> {});
    }
}
