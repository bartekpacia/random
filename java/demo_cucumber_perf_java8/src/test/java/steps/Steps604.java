package steps;

import io.cucumber.java8.En;

public class Steps604 implements En {

    public Steps604() {
        When("^the step is implemented604$", () -> {});
        When("^the step is not implemented yet604$", () -> {});
        Then("^the next step is executed604$", () -> {});
        Then("^the next step is skipped604$", () -> {});
    }
}
