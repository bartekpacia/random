package steps;

import io.cucumber.java8.En;

public class Steps636 implements En {

    public Steps636() {
        When("^the step is implemented636$", () -> {});
        When("^the step is not implemented yet636$", () -> {});
        Then("^the next step is executed636$", () -> {});
        Then("^the next step is skipped636$", () -> {});
    }
}
