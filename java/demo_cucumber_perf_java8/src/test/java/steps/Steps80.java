package steps;

import io.cucumber.java8.En;

public class Steps80 implements En {

    public Steps80() {
        When("^the step is implemented80$", () -> {});
        When("^the step is not implemented yet80$", () -> {});
        Then("^the next step is executed80$", () -> {});
        Then("^the next step is skipped80$", () -> {});
    }
}
