package steps;

import io.cucumber.java8.En;

public class Steps743 implements En {

    public Steps743() {
        When("^the step is implemented743$", () -> {});
        When("^the step is not implemented yet743$", () -> {});
        Then("^the next step is executed743$", () -> {});
        Then("^the next step is skipped743$", () -> {});
    }
}
