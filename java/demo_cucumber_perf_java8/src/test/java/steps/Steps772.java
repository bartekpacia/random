package steps;

import io.cucumber.java8.En;

public class Steps772 implements En {

    public Steps772() {
        When("^the step is implemented772$", () -> {});
        When("^the step is not implemented yet772$", () -> {});
        Then("^the next step is executed772$", () -> {});
        Then("^the next step is skipped772$", () -> {});
    }
}
